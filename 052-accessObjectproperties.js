// Here we will learn how to access object properties
let point = {
    x:10,
    y:20,
    z:45
}
console.log (point.x) //another way (point['x'])
console.log(point.x+point.y)

//another way is 
let show = 'z'
console.log(point[show])
